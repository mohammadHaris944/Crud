
<h1>Showing all articles </h1>


<table>
<thead>
<tr>
<th >Title</th>

<th>Descriptions</th>
<th colspan="3">Actions</th>

</tr>

</thead>
<tbody>
<% @articles.each do |item| %>
    <tr>
        <td><%= item.title  %></td>
        <td><%= item.description  %></td>
        <td> <%=link_to 'Show',article_path(item)%></td>
        <td> <%=link_to 'Edit',edit_article_path(item)%></td>    <!--you can check it from rails routes --expanded  in prefix path is written-->
        <td> <%=link_to 'Delete',article_path(item), method: :delete,data: {confirm: "Are You sure?"}%></td>  <!--- here method shows delete request ,i we don't specified method name it will take as Get request and show currently selected item--->
        
    </tr>
<% end %>
</tbody>

</table>
<br>
<p ><%= link_to 'Create New Article',new_article_path%><p>
